global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: consumer-service
    metrics_path: /metrics
    static_configs:
      - targets: ["consumer-service:8001"]
